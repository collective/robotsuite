[buildout]
extends = buildout.cfg
parts =
   packages
   test

[packages]
recipe = hexagonit.recipe.download
url = https://dist.plone.org/releases/${versions:Plone}/packages.tar.gz
destination = ${buildout:eggs-directory}
