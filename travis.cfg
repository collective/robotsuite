[buildout]
extends = buildout.cfg
parts =
   packages
   test
download-cache = ${buildout:eggs-directory}

[packages]
recipe = hexagonit.recipe.download
url = http://dist.plone.org/release/${versions:Plone}/packages.tar.gz
destination = ${buildout:download-cache}
